<main class="container-fluid">
  <div class="row">
    <div class="container">
    <div class="col-sm-12 mt-3 typewriter">
      <h1>Portfolio</h1>
      <hr>
    </div>
  </div>
  </div>

  <!--Begin album content 
        Note Bootstrap documentation allows for column wrapping.  See this link:  https://getbootstrap.com/docs/4.0/layout/grid/#auto-layout-columns
        If more than 12 columns are placed within a single row, each group of extra columns will, as one unit, wrap onto a new line.
        ***By Design, to make it easy to add content from a database in the future, I am choosing one row which will wrap all cards in the album when they exceed 12 columns***
      -->

  <div class="album py-2 bg-light">
    <div class="container">
      <div class="row">

        {{#each portfolio}}
        <!--1st card-->
        <div class="col-md-4 col-sm-6 col-xs-12">
          <div class="card mb-4 shadow-sm">
            <div class="card-body p-0 img-overlay">
              <img class="img-fluid card-img portfolio-thumbs" src={{this.src}} alt="{{this.title}} Card image">
              <div class="p-0 m-0 card-img-overlay d-flex border border-white">
                <div class="d-flex justify-content-center align-items-top caption-container">
                  <span class="caption text-right w-100 pt-0 pr-0 mr-0">{{this.caption}}</span>
                </div>
              </div>
            </div>
            <div class="card-body">
              <h5 class="card-title">{{this.title}}</h5>
              <p class="card-text">{{this.description}}</p>
              <hr>
              <h6 class="card-subtitle mb-2 text-muted">Technology:
                <h6>{{#each this.tags as |item|}}<span class="small ml-1"> {{item}} </span>{{/each}}</h6>
            </div>            
            <div class="card-footer bg-white">
              <div class="row d-flex justify-content-center text-center">
                <button class="btn btn-outline-secondary m-1">
                  <a class="nav-link portfolio-link py-0" href={{this.githublink}} target="_blank">
                    <i class="fab fa-github"></i>
                    GitHub
                    <i class="fas fa-code"></i>
                  </a>
                </button>

                {{#if this.deployed}}
                <button class="btn btn-outline-secondary m-1" data-toggle="tooltip" data-html="true" title="<em>{{this.tooltip}}</em>">
                  <a class="nav-link portfolio-link portfolio-link py-0" href={{this.deploylink}}
                    target="_blank">
                    <i class="fas fa-desktop"></i>
                    Deployed
                    <i class="{{this.fontawesome}}"></i>
                  </a>
                </button>
                {{/if}}  


                
 

              </div>
            </div>
          </div>
        </div>
        {{/each}}
      </div>
    </div>
  </div>

</main>